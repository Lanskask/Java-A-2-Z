Вопросы по Курсу Java. №1. 

В одном из тестов у меня был вопрос компилируемый или интерпретируемый язык Java. Было два варианта. Соответственно: компилируемый или интерпретируемый. Не было даже вариантов не то, не то или варианта - и то, и то. Что отвечать в таких случаях в тесте где только два варианта ответа? 
я знаю что Java не компилируется т.к. не выполняется на процессоре непосредственно, а на JVM. 

В первой лекции должна быть команда 
javac -s parsentev\src -d parsentev\out parsentev\src\Calculate.java
если запускать из папки java-a-2-z
а у тебя (мы же не ты, да?) 
javac -s parsentev\src -d parsentev\out parsentev\Calculate.java
т.е. без src\ перед Calculate.java

Почему используется папка M2 для Maven, а не M3
Почему в видео M2_HOME, а не M3_HOME, ведь там версия Maven'a 3.х ?

